{
  "name": "spinal-plug",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm build && pnpm -r --if-present test"
  },
  "devDependencies": {
    "@types/node": "^24.0.13",
    "typescript": "^5.9.2"
  }
}
