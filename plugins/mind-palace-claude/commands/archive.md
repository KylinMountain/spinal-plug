---
description: Create a named Mind Palace archive for the current non-Git workspace.
---

The user has chosen to create a new archive for this directory. Use the name they provide; otherwise use the directory name:

```bash
mind-palace archive "$HOME/.mind-palace/mind-palace.db" . "<archive-name>"
```

Report that this directory will automatically load the selected archive in future sessions.
