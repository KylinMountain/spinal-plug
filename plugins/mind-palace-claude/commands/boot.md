---
description: Display the Mind Palace memory-core loading sequence for the current project.
---

Run the current project's Mind Palace Memory Core Boot Sequence:

```bash
mind-palace boot "$HOME/.mind-palace/mind-palace.db" .
```

Report the output exactly as a short system-status sequence. In the current project-only release, `Mind Capsule` means a project-scope boot context, and `Memory Fidelity` reports loaded durable-memory references rather than an invented percentage. Do not imply that the Agent's model weights or hidden state were copied; this indicates Project Space, durable memory, context projection, and local sync readiness only.
